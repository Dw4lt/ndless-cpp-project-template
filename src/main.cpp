#include <stdio.h>
#include <string>
#include <time.h>

int main() {
    srand(time(NULL)); // Just a test to see if the Ndless sources are available
    printf("Hello world!\n");
    return EXIT_SUCCESS;
}
